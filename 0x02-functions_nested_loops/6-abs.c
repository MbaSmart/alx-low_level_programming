#include "main.h"

/**
* _abs -> Write a function that computes the
* absolute value of an integer.
*
* @n: This indicates an integer
*
* Return: 0 (Success)
*/

int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
