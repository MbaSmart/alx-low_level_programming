#include "main.h"

/**
* _isupper -> This is a function that checks
* for uppercase character.
*
* @c: is a character
*
* Return: 0 (Success)
*/

int _isupper(int c)
{
	if (c <= 'Z' && c >= 'A')
	{
		return (1);
	}
	else

	return (0);
}
